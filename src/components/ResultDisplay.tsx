import { MainWord } from "./MainWord"
import { WordResult } from "./WordResult"

export const ResultDisplay = () => {

    return (
        <section>
            <MainWord />
            <WordResult />
            <WordResult />
        </section>
    )
}
